% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_size_spectrum.R
\name{calc_size_spectrum}
\alias{calc_size_spectrum}
\title{Calculate Normalized Biomass Size Spectrum (NBSS) Slope}
\usage{
calc_size_spectrum(
  atl.dir,
  param.dir,
  fgs.file,
  aggregate_spatial = TRUE,
  log10_bin_width = 0.5
)
}
\arguments{
\item{atl.dir}{Character. Path to Atlantis output directory}

\item{param.dir}{Character. Path to Atlantis parameter directory}

\item{fgs.file}{Character. Path to the functional groups CSV file}

\item{aggregate_spatial}{Logical. If TRUE, aggregates biomass system-wide
across all polygons before calculating a global slope. If FALSE, calculates
the NBSS slope per polygon. Defaults to TRUE.}

\item{log10_bin_width}{Numeric. The width of the weight bins in log10 space.
Defaults to 0.5 (half an order of magnitude).}
}
\value{
A data frame containing the size spectrum slope (\code{size.spectrum.slope}),
R-squared (\code{r2}), and intercept (\code{intercept}) per time step.
}
\description{
This function extracts abundance and weight data from Atlantis outputs,
calculates total biomass per group, bins the ecosystem into logarithmic
weight classes, isolates the descending limb, and calculates the NBSS slope.
}
